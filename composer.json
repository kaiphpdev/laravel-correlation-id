{
    "name": "kaiphpdev/laravel-correlation-id",
    "description": "End-to-end correlation ID tracing for Laravel HTTP requests, logs, outgoing HTTP calls, and queued jobs.",
    "type": "library",
    "license": "MIT",
    "keywords": [
        "laravel",
        "correlation-id",
        "request-id",
        "tracing",
        "logging",
        "queue",
        "http-client"
    ],
    "authors": [
        {
            "name": "Kuldeep"
        }
    ],
    "require": {
        "php": "^8.2",
        "illuminate/contracts": "^11.0|^12.0",
        "illuminate/http": "^11.0|^12.0",
        "illuminate/log": "^11.0|^12.0",
        "illuminate/queue": "^11.0|^12.0",
        "illuminate/routing": "^11.0|^12.0",
        "illuminate/support": "^11.0|^12.0"
    },
    "require-dev": {
        "laravel/pint": "^1.0",
        "orchestra/testbench": "^9.0|^10.0",
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "psr-4": {
            "LaravelCorrelationId\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "LaravelCorrelationId\\Tests\\": "tests/"
        }
    },
    "extra": {
        "laravel": {
            "providers": [
                "LaravelCorrelationId\\CorrelationIdServiceProvider"
            ],
            "aliases": {
                "CorrelationId": "LaravelCorrelationId\\Facades\\CorrelationId"
            }
        }
    },
    "scripts": {
        "test": "phpunit",
        "format": "pint",
        "format-check": "pint --test",
        "validate-composer": "composer validate --strict",
        "check": [
            "@validate-composer",
            "@format-check",
            "@test"
        ]
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}