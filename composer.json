{
    "name": "kaiphpdev/laravel-correlation-id",
    "description": "Lightweight end-to-end correlation ID tracing for Laravel applications.",
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "Kai"
        }
    ],
    "require": {
        "php": "^8.2",
        "illuminate/support": "^11.0|^12.0"
    },
    "require-dev": {
        "orchestra/testbench": "^9.0|^10.0",
        "phpunit/phpunit": "^11.0",
        "laravel/pint": "^1.30"
    },
    "autoload": {
        "psr-4": {
            "LaravelCorrelationId\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "LaravelCorrelationId\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "format": "pint",
        "format-check": "pint --test",
        "validate": "composer validate --strict",
        "check": [
            "@validate",
            "@format-check",
            "@test"
        ]
    },
    "extra": {
        "laravel": {
            "providers": [
                "LaravelCorrelationId\\CorrelationIdServiceProvider"
            ],
            "aliases": {
                "CorrelationId": "LaravelCorrelationId\\Facades\\CorrelationId"
            }
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}